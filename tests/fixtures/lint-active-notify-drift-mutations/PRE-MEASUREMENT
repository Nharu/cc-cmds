# 사전 측정 블록 — 아래 행들의 벡터가 어느 픽스처 집합에 대해 도출됐는지 고정한다.
# 값은 리비전이 아니라 **내용 해시**다. 이 하네스를 다시 돌리라고 문서화된 발동
# 조건이 「대상이 바뀌면」이고 그것은 곧 더러운 트리인데, 리비전 검사는 표를
# 무효화하는 바로 그 경우에 통과한다. 이름 목록만으로도 부족하다 — 픽스처의
# **내용**만 고치면 이름 해시는 그대로인데 한 행의 벡터가 무너진다.
#
# 불일치 시 할 일: 벡터를 현재 집합에 대해 **다시 도출**하고 이 블록을 갱신한다.
# 관측에 맞춰 벡터를 고쳐 쓰는 것은 금지된다.
fixture-set-content-sha256: 1c852a44cc90a8bafb850725b5f938e2e78bef2d6be258a15c29223e76b9cb4b
fixture-count: 35
recorded: 2026-08-14
