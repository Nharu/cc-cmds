#!/usr/bin/env bash
# The defect itself: a title literal opening with a bracket. The banner appears,
# the title does not — the application's own name takes its place.
terminal-notifier -title "[cc-cmds] 손 필요" -message "승인을 기다립니다" -execute ':'
