#!/usr/bin/env bash
# A leading space is no shield for the five the value parser judges — it strips
# whitespace first, so this is swallowed exactly as an unspaced bracket is.
terminal-notifier -title "cc-cmds 손 필요" -message " [경고] 확인이 필요합니다" -execute ':'
