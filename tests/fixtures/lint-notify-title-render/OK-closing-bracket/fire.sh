#!/usr/bin/env bash
# Closing brackets pass. A lint that banned punctuation wholesale would be
# refusing text the tool renders perfectly well.
terminal-notifier -title "cc-cmds 자율 런" -message ")x 닫는 괄호는 통과한다" -execute ':'
