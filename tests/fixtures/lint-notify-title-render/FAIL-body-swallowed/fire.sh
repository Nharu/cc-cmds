#!/usr/bin/env bash
# The same swallowing on the body side, which was only ever half-guarded: the
# old rule knew about `[` and nothing else.
terminal-notifier -title "cc-cmds 손 필요" -message "(임시) 확인이 필요합니다" -execute ':'
