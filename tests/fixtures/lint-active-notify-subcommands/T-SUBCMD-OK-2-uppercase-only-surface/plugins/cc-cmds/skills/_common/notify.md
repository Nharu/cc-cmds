# Active Notify (Shared Procedure)

  The lifecycle phases are ARM, FIRE-NOW and CANCEL. Naming them in the phase
  register is still naming them, so rule 1 must accept this surface — it is
  case-insensitive on purpose, because several sites legitimately write the
  phase rather than the subcommand.
