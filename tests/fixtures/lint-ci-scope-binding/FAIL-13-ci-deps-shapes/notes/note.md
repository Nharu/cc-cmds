The filter wakes this file and no suite reads it, and no `# ci-subject:` claims
it either. That is rule 2 — the largest rule by output on a real tree, and the
one no fixture asserted before this one.
