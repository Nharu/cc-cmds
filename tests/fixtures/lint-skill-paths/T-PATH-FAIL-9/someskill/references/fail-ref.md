# References file violation (depth-3 scan)

This file lives at `<skill>/references/<name>.md` and must be picked up by the
third find branch. It contains `~/.claude/teams/foo` to trigger a violation.
