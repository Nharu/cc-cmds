# Violation in SKILL.md location

This file lives at `<skills_root>/<skill>/SKILL.md` and contains `~/.claude/teams/foo`,
which must be detected by the SKILL.md branch of the file collection — fail lint.
