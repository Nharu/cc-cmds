# Active Notify (Shared Procedure)

Event-scoped repeat lifecycle.

The citation below names a real section but carries no title anchor, so a
later renumbering would move it silently. Unanchored citations fail.

fire-now is the only dispatch surface (SKILL.md §4.4); when unsure an ARM
is live it fires regardless (SKILL.md §4.5 Defensive fire-now).

Termination is user CANCEL or model self-cancel.
