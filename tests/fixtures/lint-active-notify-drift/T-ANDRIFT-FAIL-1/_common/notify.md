# Active Notify (Shared Procedure)

Single (mode=single) or per-turn repeat (mode=repeat) lifecycle.

fire-now is the only dispatch surface (SKILL.md §4.4 Fire-now ordering
within the turn); when unsure it fires regardless (SKILL.md §4.5
Defensive fire-now).

Bypass path per SKILL.md §7 Permission test bypass.

Termination is user CANCEL or model self-cancel.
