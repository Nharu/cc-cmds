# Active Notify (Shared Procedure)

Event-scoped repeat lifecycle.

Every cited section number below EXISTS as a heading in the owning
SKILL.md, so an existence-only citation check passes this file with zero
findings. Both citations are nonetheless wrong: each is shifted by one.
This fixture is the control that keeps the title-anchor comparison from
being "simplified" back to an existence check.

fire-now is the only dispatch surface, called ahead of any verification
that could strand the banner (SKILL.md §4.5 Fire-now ordering within the
turn); when unsure an ARM is live it fires regardless (SKILL.md §4.6
Defensive fire-now).

Bypass path per SKILL.md §7 Permission test bypass.

Termination is user CANCEL or model self-cancel.
