# 재도출(2026-08-17): 처음 예측은 세 픽스처였고 관측이 하나를 돌려줬다. 4칸 들여쓴
# arm 픽스처는 추출기를 되돌려도 붉지 않는다 — 그 픽스처의 사용법 문자열 자체가
# 새 이름을 담은 대체 표기라, 추출기가 그 arm을 놓쳐도 규칙 2가 별개 이유로 걸려
# 같은 종료 코드가 나온다. 들여쓴 esac 픽스처도 붉지 않는다 — 이 변이는 arm 인식
# 조건만 되돌리고 깊이 추적은 그대로 두기 때문이다.
T-SUBCMD-ERR-2-unrecognized-arm
