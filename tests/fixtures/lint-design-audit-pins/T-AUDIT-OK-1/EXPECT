# The arity pin: every pinned group size is named on this one line, so dropping
# any single pinned literal changes a number here and turns this fixture red.
OK:   design-audit pins — 6 constants (CFI body) + 4 reader-prompt + 1 measurement + 3 verdict tokens x2 regions + 6 reference + 15 disclosure (in-fence, ordered) + 2 shortfall arm + 11 denylist (in CFI-6) all intact
