# The arity pin: every pinned group size is named on this one line, so dropping
# any single pinned literal changes a number here and turns this fixture red.
# The span floors are named by VALUE for the same reason — an array size cannot
# see a floor being lowered, because the array still has three entries.
OK:   design-audit pins — 6 constants (CFI body, value + sole assignment) + 1 reader-prompt preamble + 3 reader-prompt body + 1 measurement + 3 verdict tokens (each checked in 2 regions) + 4 reference headings (whole-line) + 5 reference sections (content + span floors 5/3/7/3/2) + 3 reference prose (region-scoped) + 2 disclosure head + 15 disclosure (in-fence, ordered) + 2 shortfall arm + 6 block arithmetic (in checks section) + 1 declared slot count (in block grammar) + 11 denylist (in CFI-6) all intact
