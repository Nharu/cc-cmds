# Reconciliation Pass (single, non-recursive)

Consumed once. Runs `ADJUSTMENT_PASSES` times by name, never by number.


No spawning. No re-opening a dispositioned finding. The pass's own edits are never new input.

## Dedup and reinforcement

Same anchor and same root cause.

## Severity re-assignment (single labeller, one pass)

Never take a maximum across readers.

## Routing — five named owners, exactly one each

| 소유자 | 조건 |
| --- | --- |
| 조정 패스 적용 | 즉시 반영 가능 |

## Synthesis question (mandatory terminal act)

One question, asked last.
