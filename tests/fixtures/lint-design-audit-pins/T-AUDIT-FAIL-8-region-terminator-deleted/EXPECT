# Both region terminators were deleted, so neither region can be read. The
# extractor fails loudly instead of widening to end of file, and every pin that
# depended on those regions reports region-unavailable rather than claiming its
# literal is missing — the literals are all still in the fixture, untouched.
# The line COUNT is what preserves each pin's attribution to this fixture: one
# line per pin, 6 constants + 11 denylist tokens + the 2 extractor diagnostics.
FAIL: design-audit/SKILL.md (CFI body) — region terminator literal missing after the start anchor: /^## Workflow[[:space:]]*$/
FAIL: design-audit/SKILL.md (constants) — region-unavailable (the '## Control-Flow Invariants' body), so this pin was not evaluated: READER_COUNT = 3
FAIL: design-audit/SKILL.md (constants) — region-unavailable (the '## Control-Flow Invariants' body), so this pin was not evaluated: ROUNDS_PER_READER = 1
FAIL: design-audit/SKILL.md (constants) — region-unavailable (the '## Control-Flow Invariants' body), so this pin was not evaluated: OUTER_ITERATIONS = 0
FAIL: design-audit/SKILL.md (constants) — region-unavailable (the '## Control-Flow Invariants' body), so this pin was not evaluated: ADJUSTMENT_PASSES = 1
FAIL: design-audit/SKILL.md (constants) — region-unavailable (the '## Control-Flow Invariants' body), so this pin was not evaluated: ROUND_TOKEN = 1
FAIL: design-audit/SKILL.md (constants) — region-unavailable (the '## Control-Flow Invariants' body), so this pin was not evaluated: PASS_TOKEN = fanout
FAIL: design-audit/SKILL.md (CFI-6 denylist) — region terminator literal missing after the start anchor: /^## Workflow[[:space:]]*$/
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: consecutive_no_major
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: COUNT_APPLIED
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: escalate_applied
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: INNER_EXIT_REASON
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: inner_round
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: outer_iter
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: outer_log.md
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: ack_items.md
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: pending_applies.md
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: INNER_TEMP_DIR
FAIL: design-audit/SKILL.md (denylist) — region-unavailable (the CFI-6 denylist body), so this pin was not evaluated: convergence_table.md
