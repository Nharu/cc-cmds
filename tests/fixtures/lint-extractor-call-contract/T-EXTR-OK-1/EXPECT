# The canonical form from the helper's own example, verbatim. If this fixture
# ever needs editing to keep passing, the example it copies is what changed.
OK:   extractor call contract — 1 call site(s) across 1 file(s), all guarded by `if` (item (1) only; the sentinel assignment of item (2) is prose-enforced)
