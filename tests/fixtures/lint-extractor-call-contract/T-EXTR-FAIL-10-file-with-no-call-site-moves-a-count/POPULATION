# This fixture's own populations. The real tree has no such file — the pins in
# the lint apply there — and a declaration beside the real tree is a failure, so
# this override cannot be used to relax the shipped pins.
EXPECTED_SITES=1
EXPECTED_SITE_FILES=2
