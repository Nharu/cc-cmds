# The substitution sits inside an `if` but is not what the `if` tests. A prefix
# test certified this shape as guarded and raised the site count — an active
# false assertion about a line it had read.
FAIL: consumer.sh:6 — unrecognized `extract_between` form; this lint could not classify it as guarded, unguarded, or prose, and an unclassified occurrence is not a covered one
