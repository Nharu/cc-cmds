# The vocabulary table region terminator was deleted. The region must fail loudly
# rather than widen to end of file — a widened region contains the pinned token
# anyway, so a silently-widening extractor reports success while the region
# scoping it was doing has quietly stopped.
FAIL: _common/verification.md (vocabulary table) — region terminator literal missing after the start anchor: /^### 3\.1 /
FAIL: _common/verification.md (SOT) — frozen literal missing from the frozen-vocabulary table: | `구현 시 검증` |
