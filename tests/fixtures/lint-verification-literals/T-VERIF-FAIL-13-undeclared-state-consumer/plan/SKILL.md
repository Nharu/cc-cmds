# plan

A third consumer, landed without a rendering declaration. It reads the
walkthrough `상태` of a design document handed to it and branches on the value,
and it says nothing about which of the two renderings it can see.
