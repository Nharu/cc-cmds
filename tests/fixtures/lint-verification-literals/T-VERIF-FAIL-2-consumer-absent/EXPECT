# The consumer file is gone. This used to report "OK … skipped" — a fence whose
# target has vanished reporting success is the same shape of defect this file
# exists to pin, so it is now a failure.
FAIL: implement/SKILL.md (consumer) — file not found; the 1.5a fence has no target and cannot report OK
