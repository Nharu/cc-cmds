# Reviewer context package (fixture stand-in)

**Which `상태` renderings this consumer reads — declared, because the key has two and the shared contract requires every consumer of it to say.** Read both: the sub-section form and the table form.
