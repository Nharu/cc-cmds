---
name: design
description: fixture
---

### Step 2: Team Composition Proposal (Korean)

- **How many roles**: bounded by the shared team size budget.

This fixture carries no gate heading: the lever is skip-if-absent, so the lint
must stay green here. The heading's absence is a reverted lever, not a
regression — the regression that removes the heading is caught by code review
of this step, not by this lint.
