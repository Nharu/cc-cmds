# Team-Upgrade Analysis (fixture)

## Scope

**Out of scope — never propose**: model downgrade; and any `REMOVE` / `MERGE` argued from cost, token budget, or headcount alone.


**`REMOVE` / `MERGE` are pre-spawn only.** Rationale.

## Output format

### `REMOVE` (drop a role whose domain no longer needs its own seat)

Fields.

### `MERGE` (two roles → one, both parents replaced)

Fields.
