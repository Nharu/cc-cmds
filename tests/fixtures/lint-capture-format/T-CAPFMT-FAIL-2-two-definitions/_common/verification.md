# Verification (fixture)

Two spellings of the format. Nothing else is wrong, and that is the point: each
line is individually correct, and the defect is that there are two of them.

```
git status --porcelain --untracked-files=all
```

Consumers run `git status --porcelain --untracked-files=all` at the boundary.
